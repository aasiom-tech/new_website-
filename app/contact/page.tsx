import { PagePlaceholder } from "@/components/PagePlaceholder";
import { pageNames } from "@/content/site";

export default function ContactPage() {
  return <PagePlaceholder title={pageNames.contact} />;
}
