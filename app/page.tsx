import { PagePlaceholder } from "@/components/PagePlaceholder";
import { pageNames } from "@/content/site";

export default function HomePage() {
  return <PagePlaceholder title={pageNames.home} />;
}
